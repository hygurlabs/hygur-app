{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window. The WebUI is served by the loopback sidecar (http://127.0.0.1:8420), a remote origin to Tauri, so it must be listed under `remote` for the SPA to call our app commands (engine-mode config).",
  "windows": [
    "main"
  ],
  "remote": {
    "urls": [
      "http://127.0.0.1:8420",
      "http://localhost:8420"
    ]
  },
  "permissions": [
    "core:default",
    "allow-get-desktop-config",
    "allow-set-desktop-config",
    "allow-open-external",
    "deep-link:default"
  ]
}
